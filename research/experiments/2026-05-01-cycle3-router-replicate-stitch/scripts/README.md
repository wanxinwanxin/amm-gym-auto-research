# Cycle-3 router-replicate-stitch — scripts

The original SQL template (`router_filtered_quantiles.sql`) was retired in
cycle 5 and lives at `_obsolete/router_filtered_quantiles.sql`. See the
banner at the top of that file for the reason.

For active router-filter SQL, follow the direct-filter pattern documented
in `research/notes/data_sources.md` §1 ("Router-filtering markout_prod").
The cycle-4 experiment results that grounded the new pattern are at
`research/experiments/2026-05-03-cycle4-router-confirmed/`.
